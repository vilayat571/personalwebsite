 if (form.email.indexOf("@") !== -1 && form.password.length > 8) {
      dispatch(signIn(form));

    } 


    else if (form.password.length < 8) {
      setMessage("password must contain minimun 8 symbol");
      setTimeout(() => {
        setMessage("");
      }, 2000);
    
    }

    else if(form.email.indexOf("@") === -1){
      setMessage("email must contain @ symbol");
      setTimeout(() => {
        setMessage("");
      }, 2000);
    }

    (typeof data === "number")